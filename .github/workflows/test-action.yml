name: test-action
on: 
  workflow_dispatch:
jobs:
  test-action:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v3
      # - uses: actions/setup-node@v3
      #   with:
      #     node-version: 18.x
      - run: yarn install
        run: yarn test
